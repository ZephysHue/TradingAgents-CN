# Existing Crypto Strategy Version Audit

## Scope

[KNOWN] This is a read-only evidence audit of the existing V1, V2, and V3 BTCUSD_PERP 15m strategy implementations and frozen reports. No strategy code, input data, parameters, historical result, or research artifact was changed.

| Version | Classification | Evidence-based conclusion |
|---|---|---|
| V1 EMA / ATR Pullback | `rejected_by_evidence` | Scenario A has only +0.043464R expectancy; Scenario D is -0.126053R, PF 0.781348, and BTC DD -95.20%. |
| V2 Structural Re-Expansion v0.1 | `rejected_by_evidence` | Scenario A is -0.033383R; Scenario D is -0.257397R, PF 0.648894, DD -99.73%. |
| V3 Compression Breakout Expansion v0.1 | `rejected_by_evidence` | Scenario A is -0.104489R; Scenario D is -0.474317R, PF 0.424161, DD -99.89%. |

## Audit findings

### V1

[KNOWN] `research/crypto_backtest/backtest.py` uses completed 1H/4H alignment, EMA trend filters, 15m ATR-zone pullback, confirmed Swing stop, 2R target, and next-bar-open entry. `audit_corrected_baseline.py` validates inverse COIN-M BTC accounting, integer contracts, cost scenarios, ledger identity, and identical signal sets.

[KNOWN] The frozen baseline has 1,858 signals. Funding is explicitly unavailable/excluded. There is a reproducible command and data manifest with archive hashes. The 70/30 result generated by the general runner is not a frozen discovery-to-validation protocol; it is insufficient to classify V1 as research-validated.

[COMPUTED] The cost drag is -0.169517R per trade from A to D, greater than the +0.043464R gross expectancy. This is direct failure evidence, not missing evidence.

### V2

[KNOWN] `research/crypto_backtest/backtest_v2_structural_reexpansion.py` is a separate structure-based entry architecture: confirmed five-bar impulse, controlled pullback, close re-expansion, completed 4H background, next-bar entry, structure stop, and 2R target. Its report asserts future leakage zero and raw-trigger/fill separation.

[KNOWN] It has no frozen independent out-of-sample validation. That absence would normally be `inconclusive`; however its full-sample Scenario A is already negative, so the baseline is rejected by direct evidence.

### V3

[KNOWN] `research/crypto_backtest/backtest_v3_compression_breakout_expansion.py` is separate from V1/V2: it uses 15m compression, range close-breakout, expansion, next-bar entry, range-failure stop, and 2R target. V3 reports raw signals separately from executable and cost-constrained trades, with zero future-leakage violations and ledger PASS.

[KNOWN] Both IID and monthly block bootstrap results are negative. V3 is rejected because gross and net evidence are negative, not because Market Edge Discovery v1 found zero candidates.

## Relation to Market Edge Discovery v1

[KNOWN] Market Edge Discovery v1 froze zero Development candidates under its specific BTCUSD_PERP 15m pre-registered feature / 1h extreme-quintile >=10bp research route. [INFERRED] This does not logically invalidate every historical strategy mechanism. It provides no extra failure evidence for V1/V2/V3 beyond their respective direct results.

## Final conclusion

[KNOWN] Preserve V1, V2, and V3 as reproducible rejected research assets and accounting-regression fixtures; stop active maintenance aimed at making any of them tradable, because each has direct negative cost-adjusted evidence and V2/V3 also fail gross-edge gates.
